#include <stdio.h>
#include <stdlib.h>
#include "main.h"

/**
 * _abs - check description
 * Description: to compute the absolute value of an integer
 * @n: number to check
 * Return: 0.
*/

int _abs(int n)
{
	int b;

	n = abs(b);
	_putchar(n);

	return (0);
}
